# 🔐 환경 변수 설정 파일
# 이 파일을 .env로 복사하고 실제 값들로 수정하세요

# 서버 설정
NODE_ENV=development
PORT=3001
HOST=localhost

# 관세청 API 설정
CUSTOMS_API_KEY=your_actual_api_key_here
CUSTOMS_API_BASE_URL=https://unipass.customs.go.kr:38010/ext/rest

# CORS 설정 (프론트엔드 도메인)
FRONTEND_URL=http://localhost:3000,http://127.0.0.1:5500,http://localhost:5500

# Rate Limiting 설정
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 캐시 설정 (초 단위)
CACHE_DURATION=300

# 로깅 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# API 타임아웃 설정 (밀리초)
API_TIMEOUT=10000

# SSL 설정 (production용)
SSL_CERT_PATH=
SSL_KEY_PATH=

# 데이터베이스 설정 (향후 확장용)
# DATABASE_URL=
# REDIS_URL= 